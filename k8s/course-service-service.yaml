apiVersion: v1
kind: Service
metadata:
  name: course-service
  labels:
    app: course-service
spec:
  selector:
    app: course-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP # Internal service, accessible only within the GKE cluster
